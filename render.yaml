services:
  - type: web
    name: stroke-predictor
    runtime: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python app.py"
    env: python
    plan: free
    autoDeploy: true